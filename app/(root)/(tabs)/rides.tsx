import { View, Text, SafeAreaView } from 'react-native';

const Rides = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Rides</Text>
      </View>
    </SafeAreaView>
  );
};

export default Rides;
